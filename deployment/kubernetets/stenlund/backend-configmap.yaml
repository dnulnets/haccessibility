apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-configuration
  labels:
    app: acc
data:
  HAPI_KEY: "/mnt/cert/tls.key"
  HAPI_CERTIFICATE: "/mnt/cert/tls.crt"
  HAPI_DATABASE: "postgresql://heatserver:heatserver@_postgres._TCP.postgis-service/heat"
  HAPI_JWT_SESSION_LENGTH: "3600"
  HAPI_PASSWORD_COST: "10"

